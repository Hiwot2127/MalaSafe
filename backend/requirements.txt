# FastAPI and Server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
gunicorn>=21.2.0
python-multipart>=0.0.6

# Database
sqlalchemy>=2.0.25
asyncpg>=0.29.0
psycopg2-binary>=2.9.9
alembic>=1.13.1

# Authentication
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt==4.0.1         # pin: passlib 1.7.4 reads bcrypt.__about__, removed in bcrypt 4.1+
python-dotenv>=1.0.0

# Validation
pydantic>=2.5.3
pydantic-settings>=2.1.0
email-validator>=2.1.0

# AI/ML
pandas>=2.1.4
numpy>=1.26.3
scikit-learn>=1.4.0
joblib>=1.3.2
lightgbm>=4.3.0       # malaria risk predictor (Poisson + quantile + cold-start)
pyarrow>=15.0.0       # parquet for feature manifest / model artifacts

# Climate fetch pipeline (Phase 4) - CHIRPS HTTP + ERA5-Land via CDS API,
# rasters aggregated against the bundled Ethiopian woreda shapefile.
# rasterio>=1.3.10      # GeoTIFF reads (CHIRPS) - bundles GDAL via wheel
# fiona>=1.9.6          # shapefile reads for zonal stats
# shapely>=2.0.4        # geometry ops behind rasterstats
# rasterstats>=0.19.0   # zonal mean per woreda polygon
# xarray>=2024.3.0      # ERA5-Land NetCDF handling
# netCDF4>=1.6.5        # NetCDF4 backend for xarray
# cdsapi>=0.7.0         # Copernicus CDS client (reads ~/.cdsapirc)
# setuptools<81         # cdsapi 0.7.0 still imports pkg_resources, removed in setuptools 81

# Utilities
python-dateutil>=2.8.2
pytz>=2023.3

# HTTP Client
httpx>=0.26.0
aiofiles>=23.2.1
requests>=2.31.0  # For testing scripts

# Monitoring and Logging
loguru>=0.7.2

# Testing (optional)
pytest>=7.4.4
pytest-asyncio>=0.23.3
pytest-cov>=4.1.0
httpx>=0.26.0
faker>=22.0.0

# Production Hardening - Security & Rate Limiting
slowapi>=0.1.9
redis>=5.0.0

# Production Hardening - Monitoring & Observability
sentry-sdk[fastapi]>=1.40.0
structlog>=23.2.0
python-json-logger>=2.0.7

# Production Hardening - Background Tasks
celery[redis]>=5.3.0
flower>=2.0.0

# Production Hardening - Caching
aioredis>=2.0.1

# Production Hardening - System Monitoring
psutil>=5.9.0
prometheus-client>=0.19.0

# PDF Export
reportlab>=4.0.0
